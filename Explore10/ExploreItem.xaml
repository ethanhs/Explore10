<UserControl x:Class="Explore10.ExploreItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="384" d:DesignWidth="384">
    <Grid>
        <Border Background="Black">
            <Border.Effect>
                <DropShadowEffect Color="Black"  Opacity="0.5"/>
            </Border.Effect>

            <Image Source="{Binding ImageSource}" Margin="1,1,1,1"/>
        </Border>
        <TextBlock Text="{Binding FileName}" Foreground="#f0f0f0"/>
    </Grid>
</UserControl>
